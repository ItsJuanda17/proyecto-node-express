# Railway deployment configuration
apiVersion: v1
kind: Service
metadata:
  name: proyecto-node-express
spec:
  selector:
    app: proyecto-node-express
  ports:
    - port: 3000
      targetPort: 3000
  type: LoadBalancer